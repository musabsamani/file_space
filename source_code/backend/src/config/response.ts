/** The types of status messages that can be returned in the response. */
export type ResponseStatusType = "info" | "success" | "warn" | "error";

/** The types of instructions that can be returned in the response. */
export type ResponseInstructionTypes = "reload" | "logout" | "login";
